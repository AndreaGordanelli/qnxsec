# python artefacts
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/

# QNX images, firmware and samples: NEVER in the repository (proprietary or heavy).
# Testing happens on local copies kept outside the working tree.
samples/
*.ifs
*.img
*.bin
